//Напишите интерфейс, который принимает математическое выражение (prompt) и
// возвращает его результат.
//Проверять выражение на корректность не требуется.

function calc(){
   return alert(eval(prompt('выражение', '2+2')));
}